[+] Create Project Directory
[+] Create a NPM Project and install all dependencies
    - dependencies
        - express mongoose dotenv bcrypt jsonwebtoken socket.io
    - Dev dependencies
        - nodemon
[+] Create express server - app.ts
[+] Folder Structure
[+] Database Setup
[+] Error Handling Setup (Global Error Handler)

[-] API & Real-Time Creation
    - Auth API's (Authentication & Authorization)
        - Register (with password hashing)
        - Login (Generate JWT Token with user role)
        - Create Auth Middleware (Protect routes & check roles: Admin/Cashier/Customer)
    
    - Category + Items API's
        - GET, POST, PUT, DELETE Menu Items
        - Apply Auth Middleware (Only Admin can POST/PUT/DELETE)

    - Table API's
        - GET, POST Table

    - Socket.io Setup (The Real-Time Engine)
        - Initialize Socket.io server alongside Express
        - Secure Socket Connection: Add Socket JWT Middleware (Authenticate handshake before connecting)
        - Handle Connection Resilience (Auto-reconnect logic)

    - Order API's & Real-Time Flow
        - POST Order (Customer places order -> Save to DB -> Emit "new_order" via Secure Socket to Admin/Cashier)
        - GET Orders (Dashboard fetches initial orders + fallback mechanism on socket reconnect)
        - PUT Order Status (Admin updates status -> Emit "status_changed" via Socket to specific Customer)

    - Payment Related API's (Razorpay)
        - Create Payment Intent API (Secured)
        - Setup Razorpay Webhook (Listen for successful payment -> Update DB -> Emit Real-time status to Dashboard)

[-] Deploy on AWS or DigitalOcean