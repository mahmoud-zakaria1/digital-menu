========================================
DIGITAL MENU / POS SYSTEM - BACKEND PLAN
========================================
[STATUS: COMPLETE]

[✓] Create Project Directory
[✓] Create a NPM Project and install all dependencies
    - dependencies
        - express mongoose dotenv bcrypt jsonwebtoken socket.io zod
        - cors cookie-parser
    - Dev dependencies
        - nodemon typescript tsx ts-node @types/*
        - jest ts-jest supertest @types/jest @types/supertest

[✓] Create express server - app.ts
    - NOTE: refactored to separate app.ts (construction) from server.ts
      (execution) to allow testing without side effects

[✓] Folder Structure
    - controllers / routes / models / types / validators / middlewares / utils / tests

[✓] Database Setup
    - MongoDB Atlas connection with DNS override + retry logic
      (handles intermittent ECONNREFUSED / TLS handshake timeouts)

[✓] Error Handling Setup (Global Error Handler)
    - Centralized handler covering: ZodError, Mongoose CastError,
      Mongoose ValidationError, duplicate key (11000), generic errors

[✓] API & Real-Time Creation
    - Auth API's (Authentication & Authorization)
        [✓] Register (with password hashing via pre-save hook)
        [✓] Login (JWT in httpOnly cookie, role included in payload)
        [✓] Auth Middleware (isVerifiedUser, isAdmin, isAdminOrCashier)
        [✓] Get Profile endpoint

    - Category API's (NOT in original plan - added for filtering)
        [✓] GET (public), POST/PUT/DELETE (Admin only)
        [✓] Delete protection (blocked if meals still reference it)

    - Meals API's (renamed from "Items")
        [✓] GET, POST, PUT, DELETE
        [✓] Admin-only mutations, public read
        [✓] References Category via ObjectId (not free-text string)
        [✓] Duplicate name prevention

    - Table API's
        [✓] GET, POST, PUT, DELETE (expanded beyond original GET/POST-only plan)
        [✓] Cancel Reservation endpoint (auto-manages reservedAt field)
        [✓] Delete protection (blocked if table has active order)

    - Socket.io Setup (The Real-Time Engine)
        [✓] Initialize Socket.io server alongside Express (shared HTTP server)
        [✓] Secure Socket Connection: JWT middleware on handshake
            (manual cookie extraction - avoided 'cookie' package due to
            moduleResolution:nodenext type incompatibility)
        [✓] Room-based tracking (track_order) with ownership/role authorization
        [✓] Connection Resilience: pingTimeout/pingInterval configured
            (client-side reconnect logic deferred to frontend phase)

    - Order API's & Real-Time Flow
        [✓] POST Order -> server-side price calculation (prevents tampering)
            -> Save to DB -> emit "new_order" to all connected clients
        [✓] GET Orders (all + by id)
        [✓] PATCH Order Status -> enforces state machine
            (pending -> preparing -> completed, cancelled is terminal)
            -> emit "status_changed" to order-specific room
        [✓] Cancel Order (owner or Admin, pending-only, blocked if already paid)
        [✓] Delete Order (Admin only, hard delete)

    - Payment Related API's
        [✓] CHANGED FROM PLAN: Paymob instead of Razorpay
            (Razorpay not available in Egypt market)
        [✓] Create Payment Intention API (Paymob Intentions API, secured)
        [✓] Paymob Webhook with HMAC signature verification + idempotency
        [✓] emit "payment_status_changed" to order room on webhook confirmation
        [✓] Order cancellation blocked once payment is confirmed
        [✓] Tested end-to-end via ngrok (real Paymob test-mode checkout flow)

[✓] Testing
    - Jest + Supertest + isolated Atlas test database
    - Orders API integration tests: server-side price calc, strict
      field rejection, state machine transitions

[✓] README.md
    - Architecture decisions, setup instructions, API overview, tech stack rationale

[✓] Deploy (Backend)
    - CHANGED FROM PLAN: Railway first (quick deploy for job applications),
      then AWS/DigitalOcean later once frontend is built (learning goal)


========================================
FRONTEND PLAN (React + Vite) - IN PROGRESS
========================================

Stack: React + Vite + TypeScript, Tailwind CSS + SASS (.scss),
       Redux Toolkit (RTK Query) with Axios instance (withCredentials: true
       for httpOnly cookie auth), React Hook Form + Zod, Socket.io-client

--- Phase 1: Setup, SCSS Config & Architecture (3 days) ---
[✓] Scaffold project: Vite + React + TypeScript
[✓] Install & configure Tailwind CSS + SASS (.scss)
[✓] Set up RTK Query with a base Axios instance
    (withCredentials: true, required for httpOnly cookie auth against the
    existing backend)
[✓] Feature-based (domain-driven) folder architecture
[✓] Global Error Boundary + unified Toast Notification system for
    API/network error handling

--- Phase 2: Authentication & Protected Routes (3 days) ---
[ ] Login / Register pages (/login, /register) with React Hook Form + Zod
[ ] Protected Route wrapper that checks `role` (Customer / Cashier / Admin)
    and redirects based on permissions
[ ] Access-denied handling for unauthorized dashboard access

--- Phase 3: Customer Journey - Menu & Cart (5 days) ---
[ ] Menu page (/menu): meals + categories, live search, quick filtering
[ ] Dynamic QR/Table handling: read table number from URL (?table=4) and
    persist it in session
[ ] Cart management: local Redux slice replicating backend's price/tax
    calculation logic (must match server-side calc exactly)
[ ] Checkout page (/checkout): payment method selection (Cash / Online
    via Paymob)

--- Phase 4: Real-Time POS Dashboards & Sockets (7 days) ---
[ ] Cashier Dashboard (/cashier):
    [ ] Live incoming-orders view (Kanban board or list view)
    [ ] Socket.io-client integration for real-time order status updates
    [ ] Audio notification (new-order beep) on "new_order" socket event
    [ ] Quick status-change action (Pending -> Preparing -> Ready -> Completed)
[ ] Admin Dashboard (/admin):
    [ ] Full CRUD UI for menu (meals + categories)
    [ ] Meal image upload & management
    [ ] Quick analytics cards (total revenue, active orders, best-selling
        meals)

--- Phase 5: Payments, Live Tracking & Polish (4 days) ---
[ ] Customer order-tracking page (/orders/:id/track): live screen that
    listens only to that order's socket room
[ ] Price reconciliation: after order creation, replace the locally
    computed cart total with the server-returned order total before
    proceeding to payment (never trust the client-side calc for the
    amount actually charged)
[ ] Paymob checkout flow: redirect + success/callback handling
[ ] Responsive design: mobile-first for Customer/Cashier, larger-screen
    layout for Admin

--- Frontend additions (raise technical bar) ---
[✓] Modular SCSS architecture (BEM or CSS Modules where needed):
        src/styles/_variables.scss   (colors, shadows)
        src/styles/_mixins.scss      (responsive breakpoints, flex/grid mixins)
        src/styles/_components.scss  (@apply rules for buttons, cards, inputs)
        src/styles/main.scss         (entry point)
[ ] Audio tones on the Cashier dashboard for new_order socket events
    (new Audio('/sounds/new-order.mp3').play())
[ ] Optimistic Updates in RTK Query for order status changes (instant UI
    update, automatic rollback + Toast error on failed request)
[ ] Skeleton loaders (Shadcn UI Skeleton or custom SCSS) instead of plain
    spinners for menu/dashboard loading states
[ ] FE-README.md (or a frontend section in the main README) covering:
    - Auth/cookie handling with CORS
    - Socket reconnect handling strategy
    - Screenshots/GIFs of dashboards and live order tracking

[ ] Connect frontend to backend APIs (all endpoints already built & tested
    via Postman)
[ ] Socket.io client integration: reconnect logic + re-join tracked-order
    rooms on reconnect


========================================
PHASE 2 / FUTURE IDEAS (documented, not built yet)
========================================
- Meal discounts / promotions
    - Decision needed: percentage vs fixed amount
    - Likely needs separate "Promotion" collection (with start/end dates)
      rather than a static field on Meal, to support real time-limited offers
- VIP / membership tiers
    - Must be a separate field (e.g. membershipTier) - NOT mixed into
      existing `role` field, which represents system permissions, not
      marketing/loyalty status
- Pagination on Order.find() / heavier list endpoints (not needed yet at
  current scale, but noted as the real future bottleneck vs data structure)
- Category existence check on Table.currentOrder reference (minor gap)


========================================
KNOWN ARCHITECTURE DECISIONS WORTH REMEMBERING
========================================
- Auth: httpOnly cookies (not Bearer tokens) - correct choice since this
  is a single web frontend talking to its own backend. Would add hybrid
  Bearer support later if a mobile app or third-party API access is added.
  -> Frontend MUST use withCredentials: true on every request (Axios/RTK
     Query base config) or auth will silently fail via CORS.
- All ObjectId string->ObjectId conversions go through a single
  toObjectId() helper (validates + converts, throws 400 on invalid format)
- All Zod validators use .strict() to reject unexpected fields
  (e.g. blocks client-sent totalPrice tampering)
  -> Frontend cart totals are for UI/UX only; the server is the source of
     truth for price calculation and re-validates on every order.
- Global error handler is the single source of truth for error response
  shape - controllers just call next(error)
  -> Frontend's global Error Boundary / Toast system should be built
     around this single response shape.
- assertUser / assertExists helper functions (TypeScript type predicates)
  eliminate repeated null checks across controllers